'use client';import {FormEvent,useEffect,useState} from 'react';import {api,Device} from '@/lib/api';
export default function Devices(){const [items,setItems]=useState<Device[]>([]);const [created,setCreated]=useState('');const load=()=>api<Device[]>('/api/devices').then(setItems);useEffect(()=>{load();const t=setInterval(load,10000);return()=>clearInterval(t)},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await api<{deviceKey:string}>('/api/devices/provision',{method:'POST',body:JSON.stringify({name:f.get('name'),type:f.get('type'),location:f.get('location')})});setCreated(r.deviceKey);e.currentTarget.reset();load()}return <><div className="top"><div><div className="title">Device Provisioning</div><div className="sub">Register broadcasters and always-on players.</div></div></div><div className="grid two"><div className="card"><h3>Provision device</h3><form className="form" onSubmit={submit}><div className="field"><label>Name</label><input name="name" required/></div><div className="field"><label>Type</label><select name="type"><option value="player">Player</option><option value="broadcaster">Broadcaster</option></select></div><div className="field"><label>Location</label><input name="location"/></div><button className="btn">Generate device key</button>{created&&<div className="card center"><div className="sub">Device key</div><div className="statv">{created}</div></div>}</form></div><div className="card"><h3>Devices</h3><table className="table"><thead><tr><th>Name</th><th>Type</th><th>Location</th><th>Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.type}</td><td>{x.location||'-'}</td><td className={x.status==='online'?'status-online':'status-offline'}>{x.status}</td></tr>)}</tbody></table></div></div></>}
