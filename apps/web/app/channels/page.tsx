'use client';import {FormEvent,useEffect,useState} from 'react';import {api,Channel} from '@/lib/api';
export default function Channels(){const [items,setItems]=useState<Channel[]>([]);const load=()=>api<Channel[]>('/api/channels').then(setItems);useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api('/api/channels',{method:'POST',body:JSON.stringify({name:f.get('name'),slug:f.get('slug'),description:f.get('description'),priority:Number(f.get('priority'))})});e.currentTarget.reset();load()}return <><div className="top"><div><div className="title">Channels</div><div className="sub">Create logical broadcast zones and priorities.</div></div></div><div className="grid two"><div className="card"><h3>Create channel</h3><form className="form" onSubmit={submit}><div className="field"><label>Name</label><input name="name" required/></div><div className="field"><label>Slug / room key</label><input name="slug" required/></div><div className="field"><label>Description</label><input name="description"/></div><div className="field"><label>Priority (1-10)</label><input name="priority" type="number" min="1" max="10" defaultValue="5"/></div><button className="btn">Create channel</button></form></div><div className="card"><h3>Configured channels</h3><table className="table"><thead><tr><th>Name</th><th>Room</th><th>Priority</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.slug}</td><td>{x.priority}</td></tr>)}</tbody></table></div></div></>}
