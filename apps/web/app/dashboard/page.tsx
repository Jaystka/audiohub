'use client';
import {useEffect,useState} from 'react';import {api,Channel,Device} from '@/lib/api';
export default function Dashboard(){const [channels,setChannels]=useState<Channel[]>([]);const [devices,setDevices]=useState<Device[]>([]);const [sessions,setSessions]=useState<any[]>([]);useEffect(()=>{Promise.all([api<Channel[]>('/api/channels'),api<Device[]>('/api/devices'),api<any[]>('/api/sessions')]).then(([c,d,s])=>{setChannels(c);setDevices(d);setSessions(s)}).catch(console.error)},[]);const live=sessions.filter(x=>x.status==='live').length;return <><div className="top"><div><div className="title">Broadcast Control Center</div><div className="sub">Realtime health and activity across your audio network.</div></div><div className="pill"><span className="dot"/> Platform healthy</div></div><div className="grid stats"><div className="card"><div className="sub">Channels</div><div className="statv">{channels.length}</div></div><div className="card"><div className="sub">Online devices</div><div className="statv">{devices.filter(x=>x.status==='online').length}</div></div><div className="card"><div className="sub">Live broadcasts</div><div className="statv">{live}</div></div><div className="card"><div className="sub">Total devices</div><div className="statv">{devices.length}</div></div></div><div style={{height:18}}/><div className="grid two"><section className="card"><h3>Channels</h3><table className="table"><thead><tr><th>Name</th><th>Priority</th><th>Status</th></tr></thead><tbody>{channels.map(c=><tr key={c.id}><td>{c.name}</td><td>{c.priority}</td><td className="status-online">Active</td></tr>)}</tbody></table></section><section className="card"><h3>Recent sessions</h3><table className="table"><thead><tr><th>Channel</th><th>Status</th><th>Started</th></tr></thead><tbody>{sessions.slice(0,8).map(s=><tr key={s.id}><td>{s.channel}</td><td>{s.status}</td><td>{new Date(s.startedAt).toLocaleString()}</td></tr>)}</tbody></table></section></div></>}
